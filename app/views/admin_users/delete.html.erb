<h1>Users delete</h1>

<%= link_to("<< Back to user listing", :action => "list", :class => "back-link")%>
<p><em><strong>Are you sure you want to delete this admin?</em>
	
	<div align="center"><%= @admin.username %></div>
	</strong></p>

<div><%= form_for(:admin, :url => {:action => "destroy", :id => @admin.id}) do |f|%></div>
<div class="form-buttons">
  <%= link_to("Cancel", :action => 'list')%> | <%= submit_tag("Delete admin") %>
</div>
<% end %>
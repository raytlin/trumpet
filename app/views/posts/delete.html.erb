<h1>Post delete</h1>

<%= link_to("<< Back to post listing", :action => "list", :class => "back-link")%>
<p><em><strong>Are you sure you want to delete this post?</em>
	
	<div align="center"><%= @post.content %></div>
	<div align="center"><%= @post.user.username %></div>
	</strong></p>

<div><%= form_for(:post, :url => {:action => "destroy", :id => @post.id}) do |f|%></div>
<div class="form-buttons">
  <%= link_to("Cancel", :action => 'list')%> | <%= submit_tag("Delete Post") %>
</div>
<% end %>

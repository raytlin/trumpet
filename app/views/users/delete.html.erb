<h1>Users delete</h1>

<%= link_to("<< Back to user listing", :action => "list", :class => "back-link")%>
<p><em><strong>Are you sure you want to delete this user?</em>
	
	<div align="center"><%= @user.username %></div>
	</strong></p>

<div><%= form_for(:user, :url => {:action => "destroy", :id => @user.id}) do |f|%></div>
<div class="form-buttons">
  <%= link_to("Cancel", :action => 'list')%> | <%= submit_tag("Delete User") %>
</div>
<% end %>